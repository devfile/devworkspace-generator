schemaVersion: 2.2.0
metadata:
  name: default-devfile
components:
  - imageName: default-devfile-image
    dockerfile:
      uri: Dockerfile
      buildContext: .
      args:
        - "ENV1=dev"
        - "PORT=3000"